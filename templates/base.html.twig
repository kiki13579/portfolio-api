<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Administration{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/admin">Portfolio Admin</a>
            <div class="collapse navbar-collapse">
                {% block ul %}{% endblock %}
            </div>
        </div>
    </nav>

    <main class="container">
        {# Affichage des messages flash de succès #}
        {% if app.session.flash_message %}
            <div class="alert alert-success">
                {{ app.session.flash_message[0] }}
            </div>
            {# On "consomme" le message pour qu'il ne s'affiche qu'une fois #}
            {% set flash = app.session.flash('flash_message') %}
        {% endif %}

        {% block body %}{% endblock %}
    </main>

</body>
</html>